extends layout

block content
  div(ng-controller="CommitCtrl")
  h1 {{commits[0].commit.author.name}} has made {{commits.length}} total commits!
  ol
    li(ng-repeat="obj in commits" ) {{obj.commit.message}}
